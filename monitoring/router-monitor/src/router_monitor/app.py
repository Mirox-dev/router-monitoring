from __future__ import annotations

import logging


def main() -> None:
    """Placeholder entrypoint until the collector contract is implemented."""
    logging.basicConfig(level=logging.INFO)
    logging.getLogger(__name__).info("router monitor scaffold is ready")

