route:
  receiver: telegram-webhook
  group_by: [alertname, router_id]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: telegram-webhook
    webhook_configs:
      - url: http://telegram_bot:8081/alerts
        send_resolved: true
