global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: [prometheus:9090]

  # The router-monitor exporter will be added when collector contracts land.
  # It remains intentionally absent from the initial scaffold.

